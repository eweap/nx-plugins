# nx-workspace-tools

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-workspace-tools` to build the library.

## Running unit tests

Run `nx test nx-workspace-tools` to execute the unit tests via [Vitest](https://vitest.dev/).
